{
    <% articles_list.forEach(function (article, index) { %>
    {
        "title": "<%= article.title %>",
        "publishd_date": "<%= article.publish_date %>",
        "content": "<%= article.content %>"
    },
    <% }) %>
}